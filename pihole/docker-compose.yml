version: '3.8'

services:
  pihole:
    image: pihole/pihole:latest
    container_name: pihole
    network_mode: "host"      # ← add this and remove any `ports:` section
    environment:
      TZ: 'Europe/Lisbon'
      WEBPASSWORD: 'your-password'
      DNS1: 1.1.1.1
      DNS2: 1.0.0.1
    volumes:
      - './etc-pihole:/etc/pihole'
      - './etc-dnsmasq.d:/etc/dnsmasq.d'
    restart: unless-stopped
